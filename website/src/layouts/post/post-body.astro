<div class="mx-auto mt-6 max-w-3xl md:mt-16">
  <div class="markdown leading-[1.625] md:text-lg md:leading-[1.555]">
    <slot />
  </div>
</div>

<style is:global>
  @reference "../../styles/index.css"

  /* @apply inside this class doesn't work, and everything breaks if it's removed */
  .markdown {
  }

  .markdown p,
  .markdown pre,
  .markdown blockquote {
    @apply my-6;
  }

  .markdown ul,
  .markdown ol {
    @apply -mt-4;
  }

  .markdown h2 {
    @apply mt-12 mb-4 text-2xl leading-[1.2] md:text-3xl;
  }

  .markdown h3 {
    @apply mt-8 mb-4 text-xl leading-[1.2] md:text-2xl;
  }
</style>
